{
  "name": "redux-saga-promises",
  "version": "1.0.0",
  "description": "",
  "private": false,
  "main": "dist/index.js",
  "module": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/warish56/redux-saga-promises"
  },
  "scripts": {
    "publish:npm": "NODE_ENV=production && rm -rf dist && mkdir dist && npx babel ./actionCreator --out-dir dist/actionCreator --copy-files && npx babel ./saga --out-dir dist/saga --copy-files && npx babel ./config.js --out-dir dist/ --copy-files && npx babel ./index.js --out-dir dist/ --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Md Warish Ali Ansari",
  "license": "ISC",
  "dependencies": {
    "add": "^2.0.6",
    "redux-saga": "^1.1.3",
    "yarn": "^1.22.4"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.6"
  }
}
